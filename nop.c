#include "monty.h"
#include <stdio.h>

/**
 * nop - function that do no thing
 *
 * @stack: parameter that point to stack_t
 *
 * @l_number: parameter that point to unsigned int
 *
 * @n: parameter that point to int
 *
 * Return: will be 0
*/
void nop(stack_t **stack, unsigned int l_number, int n)
{
    (void)stack;
    (void)l_number;
    (void)n;
}